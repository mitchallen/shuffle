{
  "name": "@mitchallen/shuffle",
  "version": "0.1.16",
  "description": "Uses Fisher-Yates to shuffle an array",
  "main": "dist/shuffle.cjs.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/shuffle.cjs.js",
    "build": "node build.js",
    "test": "npm run build && node --test 'test/**/*.js'",
    "coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-lines=100 --test-coverage-branches=100 --test-coverage-functions=100 --test-coverage-exclude='test/**' 'test/**/*.js'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchallen/shuffle.git"
  },
  "author": "Mitch Allen <npm@mitchallen.com> (http://mitchallen.com)",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
